# Synthetic counting v24.5

V24.5 is a supervision-density control for v24.4. It keeps the model,
maximum-entropy set/count sampler, component-normalized loss, optimizer, seed,
count support, trace grammar, and 10,000-step schedule fixed. The only
substantive change is reducing the needle pool from 100 sets to 20.
